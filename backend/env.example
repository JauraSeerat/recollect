# ==================== DATABASE ====================
# Railway auto-sets this when you add PostgreSQL
# For local development, use SQLite (no need to set this)
DATABASE_URL=postgresql://user:password@localhost:5432/notesdb

# ==================== SERVER ====================
# Railway auto-sets this
PORT=8000

# CORS - Allow these origins to access your API
# Use * for development, specific domains for production
ALLOWED_ORIGINS=http://localhost:3000,https://yourapp.com,https://*.railway.app

# ==================== CLOUDFLARE R2 STORAGE ====================
# Get these from Cloudflare R2 Dashboard
# If not set, app will use local storage instead

# R2 Endpoint URL (from R2 dashboard)
R2_ENDPOINT=https://xxxxxxxxxxxxx.r2.cloudflarestorage.com

# R2 API Credentials (from R2 API Tokens)
R2_ACCESS_KEY_ID=your-access-key-id-here
R2_SECRET_ACCESS_KEY=your-secret-access-key-here

# R2 Bucket Name (the bucket you created)
R2_BUCKET=notes-app-uploads

# Optional: Custom domain for R2 (if you set one up)
# R2_PUBLIC_URL=https://cdn.yourdomain.com

# ==================== LOCAL STORAGE ====================
# Used if R2 is not configured
# Railway: use /app/uploads (persistent volume)
# Local: use ./uploads
UPLOAD_DIR=/app/uploads

# ==================== SECURITY ====================
# Change this in production!
SECRET_KEY=your-super-secret-key-change-this-in-production

# ==================== OPTIONAL SETTINGS ====================
# Python unbuffered output (good for logs)
PYTHONUNBUFFERED=1

# Node version (for Railway)
NODE_VERSION=18